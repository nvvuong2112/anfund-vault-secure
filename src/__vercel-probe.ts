// Probe tạm thời: lỗi kiểu mà vite build không quan tâm (esbuild chỉ bóc kiểu).
// Dùng để chứng minh Vercel có thật sự chạy script vercel-build hay không.
export const probe: number = "chuỗi, không phải số";
